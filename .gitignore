# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# --- Generated data and artefacts ---
data/
out/
*.hdf5
*.mp4
*.png
!assets/**/*.png

# README media is tracked on purpose
!media/
!media/*.gif

# --- Agent-authored working notes (kept locally, not committed) ---
# The measured rationale behind every physical constant lives inline in
# src/so101_sim/constants.py, and the HDF5 schema in recorder.py's docstring,
# so the code stays self-documenting without these.
docs/
CLAUDE.md
MEMORY.md

# --- OS ---
.DS_Store
logs/

# ACT training runs: multi-hundred-MB checkpoints, fully reproducible from
# config.json + norm_stats.json + splits.json + the dataset. Never commit.
runs/
runs_remote/
